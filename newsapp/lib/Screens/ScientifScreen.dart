import 'package:flutter/material.dart';

class ScienticScreen extends StatelessWidget {
  const ScienticScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'ScienticScreen',
        style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
      ),
    );
  }
}
